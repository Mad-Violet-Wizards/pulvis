Features:
* Workspace clean should also clear rtti cache
* Add check for exclusive tags in RTTI
* Add error system for RTTI
* Pak filesystem
* Introduce tests for each serialize trait type
* RTTI produces .inl with direct set/get methods.
* Fix "user/imgui.ini"
* Log Service should dump files as soon as filesystem is mounted
* Template/Preset/Inputs are in EAssetType -- they shouldn't, figure out way to load them as "Script" and filter later
* Components should be registered automatically via RTTI
* Multiple InstantiateTemplate methods & WrapEntity in CWorld -- it should disappear, as it makes the code harder to understand.
* Imgui initialized in game-engine?
Bugs:
* pulvis-rtti_rtti_autogen what is this?

Scope:
* Animations
* RTTI expansion (scripts, setters/getters, optimization, editor visibility, dll library, namings -- look into code generated by animations)
* Rework project libraries to be more "monolith"
* ImGui libraries sync
* Animations on tiles
* Simple refactors
* Tests coverage
* Editor (Level - Paint tile, erase tile, select area, fill area, remove selected area) Tileset picker, undo/redo commands, Animations, Level hierarchy, Entity inpsector, game simulation)
* lua namespaces clean up
* Threads sync 1
* Physics (velocity, acceleration, friction, gravity, collisions + layers, line trace)
* Audio (ffmpeg/wwise tests)
* Audio implementation
* Physics + audio layer covered in editor
* AI R&D
* AI implementation
* UI library
* UI library editor coverage
* particles support
* Pak filesystem support
* Threads sync 2
* Steam support
* Linux support
* Release game support
* Network (servers, p2p replication, tcp/UDP support, database support, JWT token, rest api support, auth, cache, async)
* Build/RTTI scripts refactor
